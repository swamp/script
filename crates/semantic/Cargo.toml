[package]
name = "swamp-script-semantic"
version = "0.0.1"
edition = "2021"

[dependencies]
swamp-script-ast = { path = "../ast", version = "0.0.1" }
swamp-script-parser = { path = "../parser", version = "0.0.1" }
seq-map = "0.0.10"
tracing = "0.1.40"
pest = "2.7.14" # TODO: Should be removed in the future


[dev-dependencies]
test-log = { version = "0.2.16", features = ["trace", "log", "color"] }
