[package]
name = "swamp-script-derive-tests"
version = "0.0.10"
edition = "2021"
license = "MIT"
repository = "https://github.com/swamp/script"
description = "tests for swamp macro"

[dependencies]
swamp-script-derive = { path = "../derive" , version = "0.0.10" }

[dev-dependencies]
swamp-script-derive = { path = "../derive" , version = "0.0.10" }
swamp-script-semantic = { path = "../semantic" , version = "0.0.10" }
swamp-script-core = { path = "../core" , version = "0.0.10" }
